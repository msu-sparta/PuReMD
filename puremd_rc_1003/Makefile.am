ACLOCAL_AMFLAGS = -I m4

if BUILD_MPI
  MPI=PuReMD
endif
if BUILD_SINGLE_GPU
  SINGLE_GPU=PuReMD-GPU
endif
if BUILD_MULTI_GPU
  MULTI_GPU=PG-PuReMD
endif

SUBDIRS = sPuReMD $(MPI) $(SINGLE_GPU) $(MULTI_GPU)
